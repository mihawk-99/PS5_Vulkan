# PS5 Vulkan test runner job queue (PPSA99988): C7 blit-destination formats.
# Upload: python3 tools/ps5_console.py push-jobs --replace PPSA99988 jobs/c7-blit-dst/queue.txt
#
# Rung round 5: docs/V0_FORMATS_AUDIT.md lists BLIT_DST as reachable work for the
# formats the driver can now encode back to. Each row fills a texture of its own
# format with a known R8G8B8A8 colour, blits it into a tiled R8G8B8A8_UNORM
# source the frame samples, and the readback has to be the colour that format's
# encode writes. v0-formats then re-reads the driver's reported bits against the
# audit's own record, and m2-solid last shows the console still renders.
hold 60
v0-blit-dst
v0-formats
m2-solid
exit
