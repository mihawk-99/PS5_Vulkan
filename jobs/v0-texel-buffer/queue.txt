# PS5 Vulkan test runner job queue (PPSA99988): the uniform texel buffers.
# Upload: python3 tools/ps5_console.py push-jobs --replace PPSA99988 jobs/v0-texel-buffer/queue.txt
#
# Blocker round 5: the 37 rows whose VK_FORMAT_FEATURE_UNIFORM_TEXEL_BUFFER_BIT no
# entry carried, fetched out of a buffer view through the texel-buffer descriptor
# type tooling/psbc/patch-descriptor-types.py gives the compiler and the V#
# driver/ps5vk_draw.c writes. One frame a format, four texels a frame, one per
# quarter of the target; the three probes are the float, unsigned and signed
# sampler classes, the signed one last because its shader is the one that has
# faulted ACO before. v0-formats (the audit mirror) and m2-solid are the
# regression.
hold 60
v0-formats-texel-buffer
v0-formats-texel-buffer-uint
v0-formats-texel-buffer-sint
v0-formats
m2-solid
exit
