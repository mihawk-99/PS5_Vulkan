# Upload: python3 tools/ps5_console.py battery PPSA99988 jobs/r88-device-memory/queue.txt
# R88, VkDeviceMemory outside the address window by default (docs/M5_PHASE_C.md).
# device-report records the heap (the 12 GiB pool) and VK_EXT_memory_budget.
# r88-memory allocates Vulkan memory through the driver's own placement until
# vkAllocateMemory refuses, reads the budget before, at the ceiling and after,
# and has the GPU write, invert and compare every word of it. The rest is the
# regression set, every test's memory now in the device memory region:
# drawing, indexed and instanced geometry, staging, uniforms, textures, render
# to texture, depth, stencil, MSAA and resolve, compute and storage images,
# texel buffers, multiview, subgroups, mip uploads, queries (a pool is a
# window kind) and events.
hold 1
device-report
r88-memory
b7-triangle
c2-indexed
c2-staging
c2-instancing
c3-uniform
d1-dynamic-ubo
c4-texture
c4-rtt
c5-depth
v0-stencil
r83-depth-stencil
c8-msaa
c8-resolve
d2-compute
d2-compute-images
r17-descriptor-array
v0-formats-sampled
v0-formats-storage-image
v0-formats-texel-buffer
v0-targets
v0-subpass
r84-multiview
r84-subgroup
c7-mip-upload
c7-blit-depth
r62-uniform-index
v0-query-full
b5-events
exit
