#version 450

// R60: a fragment shader whose compiled code is far larger than the 20 KiB the
// runner's fixed stage layout allowed (Dolphin's ubershaders were 24 KiB). A
// thousand distinct terms the compiler cannot fold feed a test whose outcome is
// fixed -- their sum is finite -- so every pixel is (0.25, 0.5, 0.75, 1).
// Generated by the R60 step; regenerate rather than edit.
layout(location = 0) out vec4 color;

void main()
{
    float sum = 0.0;
    sum += sin(gl_FragCoord.x * 0.001000 + 0.0000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.001700 + 0.3700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.002400 + 0.7400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.003100 + 1.1100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.003800 + 1.4800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.004500 + 1.8500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.005200 + 2.2200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.005900 + 2.5900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.006600 + 2.9600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.007300 + 3.3300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.008000 + 3.7000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.008700 + 4.0700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.009400 + 4.4400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.010100 + 4.8100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.010800 + 5.1800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.011500 + 5.5500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.012200 + 5.9200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.012900 + 0.0100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.013600 + 0.3800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.014300 + 0.7500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.015000 + 1.1200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.015700 + 1.4900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.016400 + 1.8600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.017100 + 2.2300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.017800 + 2.6000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.018500 + 2.9700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.019200 + 3.3400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.019900 + 3.7100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.020600 + 4.0800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.021300 + 4.4500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.022000 + 4.8200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.022700 + 5.1900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.023400 + 5.5600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.024100 + 5.9300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.024800 + 0.0200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.025500 + 0.3900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.026200 + 0.7600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.026900 + 1.1300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.027600 + 1.5000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.028300 + 1.8700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.029000 + 2.2400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.029700 + 2.6100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.030400 + 2.9800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.031100 + 3.3500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.031800 + 3.7200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.032500 + 4.0900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.033200 + 4.4600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.033900 + 4.8300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.034600 + 5.2000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.035300 + 5.5700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.036000 + 5.9400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.036700 + 0.0300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.037400 + 0.4000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.038100 + 0.7700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.038800 + 1.1400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.039500 + 1.5100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.040200 + 1.8800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.040900 + 2.2500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.041600 + 2.6200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.042300 + 2.9900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.043000 + 3.3600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.043700 + 3.7300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.044400 + 4.1000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.045100 + 4.4700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.045800 + 4.8400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.046500 + 5.2100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.047200 + 5.5800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.047900 + 5.9500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.048600 + 0.0400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.049300 + 0.4100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.050000 + 0.7800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.050700 + 1.1500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.051400 + 1.5200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.052100 + 1.8900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.052800 + 2.2600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.053500 + 2.6300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.054200 + 3.0000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.054900 + 3.3700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.055600 + 3.7400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.056300 + 4.1100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.057000 + 4.4800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.057700 + 4.8500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.058400 + 5.2200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.059100 + 5.5900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.059800 + 5.9600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.060500 + 0.0500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.061200 + 0.4200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.061900 + 0.7900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.062600 + 1.1600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.063300 + 1.5300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.064000 + 1.9000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.064700 + 2.2700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.065400 + 2.6400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.066100 + 3.0100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.066800 + 3.3800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.067500 + 3.7500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.068200 + 4.1200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.068900 + 4.4900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.069600 + 4.8600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.070300 + 5.2300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.071000 + 5.6000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.071700 + 5.9700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.072400 + 0.0600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.073100 + 0.4300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.073800 + 0.8000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.074500 + 1.1700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.075200 + 1.5400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.075900 + 1.9100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.076600 + 2.2800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.077300 + 2.6500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.078000 + 3.0200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.078700 + 3.3900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.079400 + 3.7600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.080100 + 4.1300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.080800 + 4.5000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.081500 + 4.8700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.082200 + 5.2400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.082900 + 5.6100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.083600 + 5.9800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.084300 + 0.0700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.085000 + 0.4400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.085700 + 0.8100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.086400 + 1.1800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.087100 + 1.5500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.087800 + 1.9200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.088500 + 2.2900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.089200 + 2.6600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.089900 + 3.0300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.090600 + 3.4000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.091300 + 3.7700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.092000 + 4.1400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.092700 + 4.5100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.093400 + 4.8800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.094100 + 5.2500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.094800 + 5.6200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.095500 + 5.9900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.096200 + 0.0800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.096900 + 0.4500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.097600 + 0.8200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.098300 + 1.1900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.099000 + 1.5600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.099700 + 1.9300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.100400 + 2.3000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.101100 + 2.6700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.101800 + 3.0400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.102500 + 3.4100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.103200 + 3.7800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.103900 + 4.1500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.104600 + 4.5200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.105300 + 4.8900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.106000 + 5.2600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.106700 + 5.6300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.107400 + 6.0000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.108100 + 0.0900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.108800 + 0.4600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.109500 + 0.8300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.110200 + 1.2000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.110900 + 1.5700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.111600 + 1.9400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.112300 + 2.3100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.113000 + 2.6800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.113700 + 3.0500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.114400 + 3.4200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.115100 + 3.7900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.115800 + 4.1600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.116500 + 4.5300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.117200 + 4.9000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.117900 + 5.2700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.118600 + 5.6400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.119300 + 6.0100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.120000 + 0.1000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.120700 + 0.4700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.121400 + 0.8400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.122100 + 1.2100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.122800 + 1.5800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.123500 + 1.9500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.124200 + 2.3200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.124900 + 2.6900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.125600 + 3.0600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.126300 + 3.4300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.127000 + 3.8000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.127700 + 4.1700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.128400 + 4.5400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.129100 + 4.9100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.129800 + 5.2800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.130500 + 5.6500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.131200 + 6.0200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.131900 + 0.1100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.132600 + 0.4800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.133300 + 0.8500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.134000 + 1.2200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.134700 + 1.5900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.135400 + 1.9600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.136100 + 2.3300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.136800 + 2.7000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.137500 + 3.0700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.138200 + 3.4400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.138900 + 3.8100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.139600 + 4.1800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.140300 + 4.5500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.141000 + 4.9200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.141700 + 5.2900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.142400 + 5.6600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.143100 + 6.0300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.143800 + 0.1200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.144500 + 0.4900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.145200 + 0.8600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.145900 + 1.2300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.146600 + 1.6000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.147300 + 1.9700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.148000 + 2.3400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.148700 + 2.7100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.149400 + 3.0800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.150100 + 3.4500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.150800 + 3.8200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.151500 + 4.1900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.152200 + 4.5600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.152900 + 4.9300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.153600 + 5.3000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.154300 + 5.6700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.155000 + 6.0400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.155700 + 0.1300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.156400 + 0.5000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.157100 + 0.8700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.157800 + 1.2400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.158500 + 1.6100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.159200 + 1.9800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.159900 + 2.3500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.160600 + 2.7200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.161300 + 3.0900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.162000 + 3.4600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.162700 + 3.8300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.163400 + 4.2000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.164100 + 4.5700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.164800 + 4.9400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.165500 + 5.3100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.166200 + 5.6800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.166900 + 6.0500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.167600 + 0.1400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.168300 + 0.5100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.169000 + 0.8800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.169700 + 1.2500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.170400 + 1.6200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.171100 + 1.9900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.171800 + 2.3600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.172500 + 2.7300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.173200 + 3.1000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.173900 + 3.4700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.174600 + 3.8400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.175300 + 4.2100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.176000 + 4.5800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.176700 + 4.9500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.177400 + 5.3200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.178100 + 5.6900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.178800 + 6.0600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.179500 + 0.1500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.180200 + 0.5200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.180900 + 0.8900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.181600 + 1.2600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.182300 + 1.6300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.183000 + 2.0000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.183700 + 2.3700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.184400 + 2.7400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.185100 + 3.1100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.185800 + 3.4800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.186500 + 3.8500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.187200 + 4.2200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.187900 + 4.5900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.188600 + 4.9600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.189300 + 5.3300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.190000 + 5.7000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.190700 + 6.0700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.191400 + 0.1600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.192100 + 0.5300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.192800 + 0.9000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.193500 + 1.2700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.194200 + 1.6400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.194900 + 2.0100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.195600 + 2.3800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.196300 + 2.7500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.197000 + 3.1200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.197700 + 3.4900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.198400 + 3.8600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.199100 + 4.2300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.199800 + 4.6000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.200500 + 4.9700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.201200 + 5.3400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.201900 + 5.7100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.202600 + 6.0800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.203300 + 0.1700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.204000 + 0.5400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.204700 + 0.9100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.205400 + 1.2800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.206100 + 1.6500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.206800 + 2.0200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.207500 + 2.3900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.208200 + 2.7600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.208900 + 3.1300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.209600 + 3.5000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.210300 + 3.8700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.211000 + 4.2400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.211700 + 4.6100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.212400 + 4.9800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.213100 + 5.3500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.213800 + 5.7200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.214500 + 6.0900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.215200 + 0.1800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.215900 + 0.5500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.216600 + 0.9200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.217300 + 1.2900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.218000 + 1.6600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.218700 + 2.0300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.219400 + 2.4000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.220100 + 2.7700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.220800 + 3.1400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.221500 + 3.5100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.222200 + 3.8800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.222900 + 4.2500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.223600 + 4.6200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.224300 + 4.9900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.225000 + 5.3600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.225700 + 5.7300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.226400 + 6.1000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.227100 + 0.1900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.227800 + 0.5600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.228500 + 0.9300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.229200 + 1.3000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.229900 + 1.6700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.230600 + 2.0400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.231300 + 2.4100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.232000 + 2.7800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.232700 + 3.1500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.233400 + 3.5200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.234100 + 3.8900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.234800 + 4.2600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.235500 + 4.6300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.236200 + 5.0000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.236900 + 5.3700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.237600 + 5.7400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.238300 + 6.1100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.239000 + 0.2000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.239700 + 0.5700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.240400 + 0.9400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.241100 + 1.3100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.241800 + 1.6800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.242500 + 2.0500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.243200 + 2.4200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.243900 + 2.7900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.244600 + 3.1600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.245300 + 3.5300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.246000 + 3.9000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.246700 + 4.2700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.247400 + 4.6400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.248100 + 5.0100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.248800 + 5.3800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.249500 + 5.7500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.250200 + 6.1200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.250900 + 0.2100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.251600 + 0.5800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.252300 + 0.9500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.253000 + 1.3200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.253700 + 1.6900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.254400 + 2.0600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.255100 + 2.4300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.255800 + 2.8000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.256500 + 3.1700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.257200 + 3.5400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.257900 + 3.9100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.258600 + 4.2800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.259300 + 4.6500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.260000 + 5.0200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.260700 + 5.3900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.261400 + 5.7600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.262100 + 6.1300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.262800 + 0.2200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.263500 + 0.5900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.264200 + 0.9600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.264900 + 1.3300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.265600 + 1.7000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.266300 + 2.0700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.267000 + 2.4400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.267700 + 2.8100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.268400 + 3.1800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.269100 + 3.5500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.269800 + 3.9200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.270500 + 4.2900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.271200 + 4.6600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.271900 + 5.0300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.272600 + 5.4000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.273300 + 5.7700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.274000 + 6.1400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.274700 + 0.2300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.275400 + 0.6000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.276100 + 0.9700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.276800 + 1.3400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.277500 + 1.7100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.278200 + 2.0800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.278900 + 2.4500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.279600 + 2.8200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.280300 + 3.1900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.281000 + 3.5600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.281700 + 3.9300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.282400 + 4.3000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.283100 + 4.6700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.283800 + 5.0400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.284500 + 5.4100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.285200 + 5.7800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.285900 + 6.1500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.286600 + 0.2400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.287300 + 0.6100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.288000 + 0.9800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.288700 + 1.3500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.289400 + 1.7200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.290100 + 2.0900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.290800 + 2.4600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.291500 + 2.8300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.292200 + 3.2000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.292900 + 3.5700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.293600 + 3.9400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.294300 + 4.3100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.295000 + 4.6800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.295700 + 5.0500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.296400 + 5.4200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.297100 + 5.7900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.297800 + 6.1600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.298500 + 0.2500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.299200 + 0.6200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.299900 + 0.9900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.300600 + 1.3600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.301300 + 1.7300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.302000 + 2.1000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.302700 + 2.4700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.303400 + 2.8400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.304100 + 3.2100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.304800 + 3.5800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.305500 + 3.9500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.306200 + 4.3200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.306900 + 4.6900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.307600 + 5.0600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.308300 + 5.4300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.309000 + 5.8000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.309700 + 6.1700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.310400 + 0.2600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.311100 + 0.6300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.311800 + 1.0000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.312500 + 1.3700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.313200 + 1.7400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.313900 + 2.1100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.314600 + 2.4800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.315300 + 2.8500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.316000 + 3.2200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.316700 + 3.5900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.317400 + 3.9600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.318100 + 4.3300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.318800 + 4.7000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.319500 + 5.0700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.320200 + 5.4400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.320900 + 5.8100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.321600 + 6.1800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.322300 + 0.2700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.323000 + 0.6400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.323700 + 1.0100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.324400 + 1.3800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.325100 + 1.7500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.325800 + 2.1200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.326500 + 2.4900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.327200 + 2.8600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.327900 + 3.2300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.328600 + 3.6000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.329300 + 3.9700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.330000 + 4.3400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.330700 + 4.7100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.331400 + 5.0800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.332100 + 5.4500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.332800 + 5.8200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.333500 + 6.1900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.334200 + 0.2800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.334900 + 0.6500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.335600 + 1.0200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.336300 + 1.3900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.337000 + 1.7600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.337700 + 2.1300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.338400 + 2.5000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.339100 + 2.8700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.339800 + 3.2400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.340500 + 3.6100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.341200 + 3.9800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.341900 + 4.3500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.342600 + 4.7200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.343300 + 5.0900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.344000 + 5.4600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.344700 + 5.8300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.345400 + 6.2000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.346100 + 0.2900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.346800 + 0.6600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.347500 + 1.0300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.348200 + 1.4000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.348900 + 1.7700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.349600 + 2.1400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.350300 + 2.5100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.351000 + 2.8800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.351700 + 3.2500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.352400 + 3.6200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.353100 + 3.9900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.353800 + 4.3600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.354500 + 4.7300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.355200 + 5.1000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.355900 + 5.4700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.356600 + 5.8400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.357300 + 6.2100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.358000 + 0.3000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.358700 + 0.6700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.359400 + 1.0400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.360100 + 1.4100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.360800 + 1.7800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.361500 + 2.1500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.362200 + 2.5200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.362900 + 2.8900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.363600 + 3.2600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.364300 + 3.6300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.365000 + 4.0000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.365700 + 4.3700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.366400 + 4.7400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.367100 + 5.1100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.367800 + 5.4800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.368500 + 5.8500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.369200 + 6.2200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.369900 + 0.3100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.370600 + 0.6800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.371300 + 1.0500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.372000 + 1.4200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.372700 + 1.7900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.373400 + 2.1600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.374100 + 2.5300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.374800 + 2.9000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.375500 + 3.2700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.376200 + 3.6400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.376900 + 4.0100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.377600 + 4.3800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.378300 + 4.7500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.379000 + 5.1200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.379700 + 5.4900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.380400 + 5.8600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.381100 + 6.2300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.381800 + 0.3200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.382500 + 0.6900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.383200 + 1.0600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.383900 + 1.4300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.384600 + 1.8000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.385300 + 2.1700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.386000 + 2.5400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.386700 + 2.9100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.387400 + 3.2800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.388100 + 3.6500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.388800 + 4.0200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.389500 + 4.3900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.390200 + 4.7600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.390900 + 5.1300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.391600 + 5.5000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.392300 + 5.8700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.393000 + 6.2400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.393700 + 0.3300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.394400 + 0.7000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.395100 + 1.0700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.395800 + 1.4400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.396500 + 1.8100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.397200 + 2.1800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.397900 + 2.5500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.398600 + 2.9200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.399300 + 3.2900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.400000 + 3.6600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.400700 + 4.0300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.401400 + 4.4000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.402100 + 4.7700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.402800 + 5.1400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.403500 + 5.5100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.404200 + 5.8800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.404900 + 6.2500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.405600 + 0.3400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.406300 + 0.7100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.407000 + 1.0800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.407700 + 1.4500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.408400 + 1.8200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.409100 + 2.1900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.409800 + 2.5600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.410500 + 2.9300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.411200 + 3.3000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.411900 + 3.6700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.412600 + 4.0400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.413300 + 4.4100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.414000 + 4.7800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.414700 + 5.1500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.415400 + 5.5200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.416100 + 5.8900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.416800 + 6.2600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.417500 + 0.3500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.418200 + 0.7200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.418900 + 1.0900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.419600 + 1.4600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.420300 + 1.8300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.421000 + 2.2000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.421700 + 2.5700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.422400 + 2.9400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.423100 + 3.3100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.423800 + 3.6800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.424500 + 4.0500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.425200 + 4.4200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.425900 + 4.7900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.426600 + 5.1600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.427300 + 5.5300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.428000 + 5.9000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.428700 + 6.2700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.429400 + 0.3600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.430100 + 0.7300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.430800 + 1.1000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.431500 + 1.4700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.432200 + 1.8400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.432900 + 2.2100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.433600 + 2.5800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.434300 + 2.9500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.435000 + 3.3200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.435700 + 3.6900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.436400 + 4.0600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.437100 + 4.4300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.437800 + 4.8000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.438500 + 5.1700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.439200 + 5.5400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.439900 + 5.9100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.440600 + 6.2800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.441300 + 0.3700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.442000 + 0.7400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.442700 + 1.1100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.443400 + 1.4800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.444100 + 1.8500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.444800 + 2.2200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.445500 + 2.5900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.446200 + 2.9600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.446900 + 3.3300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.447600 + 3.7000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.448300 + 4.0700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.449000 + 4.4400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.449700 + 4.8100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.450400 + 5.1800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.451100 + 5.5500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.451800 + 5.9200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.452500 + 0.0100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.453200 + 0.3800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.453900 + 0.7500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.454600 + 1.1200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.455300 + 1.4900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.456000 + 1.8600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.456700 + 2.2300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.457400 + 2.6000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.458100 + 2.9700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.458800 + 3.3400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.459500 + 3.7100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.460200 + 4.0800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.460900 + 4.4500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.461600 + 4.8200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.462300 + 5.1900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.463000 + 5.5600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.463700 + 5.9300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.464400 + 0.0200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.465100 + 0.3900) * 1.650;
    sum += sin(gl_FragCoord.x * 0.465800 + 0.7600) * 1.780;
    sum += sin(gl_FragCoord.x * 0.466500 + 1.1300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.467200 + 1.5000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.467900 + 1.8700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.468600 + 2.2400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.469300 + 2.6100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.470000 + 2.9800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.470700 + 3.3500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.471400 + 3.7200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.472100 + 4.0900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.472800 + 4.4600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.473500 + 4.8300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.474200 + 5.2000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.474900 + 5.5700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.475600 + 5.9400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.476300 + 0.0300) * 1.000;
    sum += sin(gl_FragCoord.x * 0.477000 + 0.4000) * 1.130;
    sum += sin(gl_FragCoord.x * 0.477700 + 0.7700) * 1.260;
    sum += sin(gl_FragCoord.x * 0.478400 + 1.1400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.479100 + 1.5100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.479800 + 1.8800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.480500 + 2.2500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.481200 + 2.6200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.481900 + 2.9900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.482600 + 3.3600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.483300 + 3.7300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.484000 + 4.1000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.484700 + 4.4700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.485400 + 4.8400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.486100 + 5.2100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.486800 + 5.5800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.487500 + 5.9500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.488200 + 0.0400) * 1.390;
    sum += sin(gl_FragCoord.x * 0.488900 + 0.4100) * 1.520;
    sum += sin(gl_FragCoord.x * 0.489600 + 0.7800) * 1.650;
    sum += sin(gl_FragCoord.x * 0.490300 + 1.1500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.491000 + 1.5200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.491700 + 1.8900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.492400 + 2.2600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.493100 + 2.6300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.493800 + 3.0000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.494500 + 3.3700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.495200 + 3.7400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.495900 + 4.1100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.496600 + 4.4800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.497300 + 4.8500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.498000 + 5.2200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.498700 + 5.5900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.499400 + 5.9600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.500100 + 0.0500) * 1.780;
    sum += sin(gl_FragCoord.x * 0.500800 + 0.4200) * 1.000;
    sum += sin(gl_FragCoord.x * 0.501500 + 0.7900) * 1.130;
    sum += sin(gl_FragCoord.x * 0.502200 + 1.1600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.502900 + 1.5300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.503600 + 1.9000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.504300 + 2.2700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.505000 + 2.6400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.505700 + 3.0100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.506400 + 3.3800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.507100 + 3.7500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.507800 + 4.1200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.508500 + 4.4900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.509200 + 4.8600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.509900 + 5.2300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.510600 + 5.6000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.511300 + 5.9700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.512000 + 0.0600) * 1.260;
    sum += sin(gl_FragCoord.x * 0.512700 + 0.4300) * 1.390;
    sum += sin(gl_FragCoord.x * 0.513400 + 0.8000) * 1.520;
    sum += sin(gl_FragCoord.x * 0.514100 + 1.1700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.514800 + 1.5400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.515500 + 1.9100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.516200 + 2.2800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.516900 + 2.6500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.517600 + 3.0200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.518300 + 3.3900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.519000 + 3.7600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.519700 + 4.1300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.520400 + 4.5000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.521100 + 4.8700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.521800 + 5.2400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.522500 + 5.6100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.523200 + 5.9800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.523900 + 0.0700) * 1.650;
    sum += sin(gl_FragCoord.x * 0.524600 + 0.4400) * 1.780;
    sum += sin(gl_FragCoord.x * 0.525300 + 0.8100) * 1.000;
    sum += sin(gl_FragCoord.x * 0.526000 + 1.1800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.526700 + 1.5500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.527400 + 1.9200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.528100 + 2.2900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.528800 + 2.6600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.529500 + 3.0300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.530200 + 3.4000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.530900 + 3.7700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.531600 + 4.1400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.532300 + 4.5100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.533000 + 4.8800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.533700 + 5.2500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.534400 + 5.6200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.535100 + 5.9900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.535800 + 0.0800) * 1.130;
    sum += sin(gl_FragCoord.x * 0.536500 + 0.4500) * 1.260;
    sum += sin(gl_FragCoord.x * 0.537200 + 0.8200) * 1.390;
    sum += sin(gl_FragCoord.x * 0.537900 + 1.1900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.538600 + 1.5600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.539300 + 1.9300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.540000 + 2.3000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.540700 + 2.6700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.541400 + 3.0400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.542100 + 3.4100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.542800 + 3.7800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.543500 + 4.1500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.544200 + 4.5200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.544900 + 4.8900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.545600 + 5.2600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.546300 + 5.6300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.547000 + 6.0000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.547700 + 0.0900) * 1.520;
    sum += sin(gl_FragCoord.x * 0.548400 + 0.4600) * 1.650;
    sum += sin(gl_FragCoord.x * 0.549100 + 0.8300) * 1.780;
    sum += sin(gl_FragCoord.x * 0.549800 + 1.2000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.550500 + 1.5700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.551200 + 1.9400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.551900 + 2.3100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.552600 + 2.6800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.553300 + 3.0500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.554000 + 3.4200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.554700 + 3.7900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.555400 + 4.1600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.556100 + 4.5300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.556800 + 4.9000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.557500 + 5.2700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.558200 + 5.6400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.558900 + 6.0100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.559600 + 0.1000) * 1.000;
    sum += sin(gl_FragCoord.x * 0.560300 + 0.4700) * 1.130;
    sum += sin(gl_FragCoord.x * 0.561000 + 0.8400) * 1.260;
    sum += sin(gl_FragCoord.x * 0.561700 + 1.2100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.562400 + 1.5800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.563100 + 1.9500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.563800 + 2.3200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.564500 + 2.6900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.565200 + 3.0600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.565900 + 3.4300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.566600 + 3.8000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.567300 + 4.1700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.568000 + 4.5400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.568700 + 4.9100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.569400 + 5.2800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.570100 + 5.6500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.570800 + 6.0200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.571500 + 0.1100) * 1.390;
    sum += sin(gl_FragCoord.x * 0.572200 + 0.4800) * 1.520;
    sum += sin(gl_FragCoord.x * 0.572900 + 0.8500) * 1.650;
    sum += sin(gl_FragCoord.x * 0.573600 + 1.2200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.574300 + 1.5900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.575000 + 1.9600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.575700 + 2.3300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.576400 + 2.7000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.577100 + 3.0700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.577800 + 3.4400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.578500 + 3.8100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.579200 + 4.1800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.579900 + 4.5500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.580600 + 4.9200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.581300 + 5.2900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.582000 + 5.6600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.582700 + 6.0300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.583400 + 0.1200) * 1.780;
    sum += sin(gl_FragCoord.x * 0.584100 + 0.4900) * 1.000;
    sum += sin(gl_FragCoord.x * 0.584800 + 0.8600) * 1.130;
    sum += sin(gl_FragCoord.x * 0.585500 + 1.2300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.586200 + 1.6000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.586900 + 1.9700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.587600 + 2.3400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.588300 + 2.7100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.589000 + 3.0800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.589700 + 3.4500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.590400 + 3.8200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.591100 + 4.1900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.591800 + 4.5600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.592500 + 4.9300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.593200 + 5.3000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.593900 + 5.6700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.594600 + 6.0400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.595300 + 0.1300) * 1.260;
    sum += sin(gl_FragCoord.x * 0.596000 + 0.5000) * 1.390;
    sum += sin(gl_FragCoord.x * 0.596700 + 0.8700) * 1.520;
    sum += sin(gl_FragCoord.x * 0.597400 + 1.2400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.598100 + 1.6100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.598800 + 1.9800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.599500 + 2.3500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.600200 + 2.7200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.600900 + 3.0900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.601600 + 3.4600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.602300 + 3.8300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.603000 + 4.2000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.603700 + 4.5700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.604400 + 4.9400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.605100 + 5.3100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.605800 + 5.6800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.606500 + 6.0500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.607200 + 0.1400) * 1.650;
    sum += sin(gl_FragCoord.x * 0.607900 + 0.5100) * 1.780;
    sum += sin(gl_FragCoord.x * 0.608600 + 0.8800) * 1.000;
    sum += sin(gl_FragCoord.x * 0.609300 + 1.2500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.610000 + 1.6200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.610700 + 1.9900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.611400 + 2.3600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.612100 + 2.7300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.612800 + 3.1000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.613500 + 3.4700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.614200 + 3.8400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.614900 + 4.2100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.615600 + 4.5800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.616300 + 4.9500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.617000 + 5.3200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.617700 + 5.6900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.618400 + 6.0600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.619100 + 0.1500) * 1.130;
    sum += sin(gl_FragCoord.x * 0.619800 + 0.5200) * 1.260;
    sum += sin(gl_FragCoord.x * 0.620500 + 0.8900) * 1.390;
    sum += sin(gl_FragCoord.x * 0.621200 + 1.2600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.621900 + 1.6300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.622600 + 2.0000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.623300 + 2.3700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.624000 + 2.7400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.624700 + 3.1100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.625400 + 3.4800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.626100 + 3.8500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.626800 + 4.2200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.627500 + 4.5900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.628200 + 4.9600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.628900 + 5.3300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.629600 + 5.7000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.630300 + 6.0700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.631000 + 0.1600) * 1.520;
    sum += sin(gl_FragCoord.x * 0.631700 + 0.5300) * 1.650;
    sum += sin(gl_FragCoord.x * 0.632400 + 0.9000) * 1.780;
    sum += sin(gl_FragCoord.x * 0.633100 + 1.2700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.633800 + 1.6400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.634500 + 2.0100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.635200 + 2.3800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.635900 + 2.7500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.636600 + 3.1200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.637300 + 3.4900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.638000 + 3.8600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.638700 + 4.2300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.639400 + 4.6000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.640100 + 4.9700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.640800 + 5.3400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.641500 + 5.7100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.642200 + 6.0800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.642900 + 0.1700) * 1.000;
    sum += sin(gl_FragCoord.x * 0.643600 + 0.5400) * 1.130;
    sum += sin(gl_FragCoord.x * 0.644300 + 0.9100) * 1.260;
    sum += sin(gl_FragCoord.x * 0.645000 + 1.2800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.645700 + 1.6500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.646400 + 2.0200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.647100 + 2.3900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.647800 + 2.7600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.648500 + 3.1300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.649200 + 3.5000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.649900 + 3.8700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.650600 + 4.2400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.651300 + 4.6100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.652000 + 4.9800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.652700 + 5.3500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.653400 + 5.7200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.654100 + 6.0900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.654800 + 0.1800) * 1.390;
    sum += sin(gl_FragCoord.x * 0.655500 + 0.5500) * 1.520;
    sum += sin(gl_FragCoord.x * 0.656200 + 0.9200) * 1.650;
    sum += sin(gl_FragCoord.x * 0.656900 + 1.2900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.657600 + 1.6600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.658300 + 2.0300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.659000 + 2.4000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.659700 + 2.7700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.660400 + 3.1400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.661100 + 3.5100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.661800 + 3.8800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.662500 + 4.2500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.663200 + 4.6200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.663900 + 4.9900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.664600 + 5.3600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.665300 + 5.7300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.666000 + 6.1000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.666700 + 0.1900) * 1.780;
    sum += sin(gl_FragCoord.x * 0.667400 + 0.5600) * 1.000;
    sum += sin(gl_FragCoord.x * 0.668100 + 0.9300) * 1.130;
    sum += sin(gl_FragCoord.x * 0.668800 + 1.3000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.669500 + 1.6700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.670200 + 2.0400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.670900 + 2.4100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.671600 + 2.7800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.672300 + 3.1500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.673000 + 3.5200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.673700 + 3.8900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.674400 + 4.2600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.675100 + 4.6300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.675800 + 5.0000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.676500 + 5.3700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.677200 + 5.7400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.677900 + 6.1100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.678600 + 0.2000) * 1.260;
    sum += sin(gl_FragCoord.x * 0.679300 + 0.5700) * 1.390;
    sum += sin(gl_FragCoord.x * 0.680000 + 0.9400) * 1.520;
    sum += sin(gl_FragCoord.x * 0.680700 + 1.3100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.681400 + 1.6800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.682100 + 2.0500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.682800 + 2.4200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.683500 + 2.7900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.684200 + 3.1600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.684900 + 3.5300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.685600 + 3.9000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.686300 + 4.2700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.687000 + 4.6400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.687700 + 5.0100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.688400 + 5.3800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.689100 + 5.7500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.689800 + 6.1200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.690500 + 0.2100) * 1.650;
    sum += sin(gl_FragCoord.x * 0.691200 + 0.5800) * 1.780;
    sum += sin(gl_FragCoord.x * 0.691900 + 0.9500) * 1.000;
    sum += sin(gl_FragCoord.x * 0.692600 + 1.3200) * 1.130;
    sum += sin(gl_FragCoord.x * 0.693300 + 1.6900) * 1.260;
    sum += sin(gl_FragCoord.x * 0.694000 + 2.0600) * 1.390;
    sum += sin(gl_FragCoord.x * 0.694700 + 2.4300) * 1.520;
    sum += sin(gl_FragCoord.x * 0.695400 + 2.8000) * 1.650;
    sum += sin(gl_FragCoord.x * 0.696100 + 3.1700) * 1.780;
    sum += sin(gl_FragCoord.x * 0.696800 + 3.5400) * 1.000;
    sum += sin(gl_FragCoord.x * 0.697500 + 3.9100) * 1.130;
    sum += sin(gl_FragCoord.x * 0.698200 + 4.2800) * 1.260;
    sum += sin(gl_FragCoord.x * 0.698900 + 4.6500) * 1.390;
    sum += sin(gl_FragCoord.x * 0.699600 + 5.0200) * 1.520;
    sum += sin(gl_FragCoord.x * 0.700300 + 5.3900) * 1.650;
    float wild = step(1.0e30, abs(sum));
    color = vec4(0.25 + wild, 0.5, 0.75, 1.0);
}
