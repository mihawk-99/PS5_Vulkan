# PS5 Vulkan c0 compute dispatch probe: the words a compute dispatch
# programs, which ps5-opengl's own libpsbc cannot report. Written by
# tools/build-compute-probe.sh with the patched compiler
# (tooling/psbc/patch-compute-metadata.py), for the runner's PC builds.
rsrc1 0x602c0001
rsrc2 0x00000006
rsrc3 0x00000000
vgprs 16
