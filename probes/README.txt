PS5 Vulkan probe definitions

These files are packaged into /app0/probes by the build. Upload the complete
generated PPSA99999 application folder to /data/homebrew/.

The host also checks /download0/probes for optional runtime overrides when that
mount is exposed by the loader. Relaunch the app after changing definitions.
